Drop the original (pre-conversion) UI chrome asset here -- currently just
logo.webp, the source for ..\textures\logo.tga.

This folder is just a staging area. It is NOT deployed to WoW and nothing
in here is read by the addon at runtime; it exists so the raw source file
has somewhere to live without ending up shipped alongside the game-ready
.tga.
